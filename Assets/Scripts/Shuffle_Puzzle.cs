
using UnityEngine;

public class Shuffle_Puzzle : MonoBehaviour
{

    public float radius = 1f;

    public float areaHeight = 0.2f;

    public bool shuffleOnStart = true;

    // PuzzlePiece[] pieces;
    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
}
